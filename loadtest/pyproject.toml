[project]
name = "metasearch-loadtest"
version = "0.1.0"
description = "Locust load tests for the metasearch-rs server"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "locust>=2.31,<3",
]

[tool.uv]
package = false
